================================================================================
  AZURE DISCOVERY TOOL - HOW TO RUN
================================================================================

🚀 QUICK START (3 STEPS):

1. LOGIN TO AZURE:
   Open PowerShell or Command Prompt and run:
   
   az login

2. RUN THE TOOL:
   
   ⭐ RECOMMENDED: Double-click this file in File Explorer:
   
      run.cmd
   
   OR from PowerShell/Command Prompt, run:
   
      run.cmd
   
   OR:
   
      .\run.cmd

3. WAIT FOR COMPLETION:
   The tool will scan your Azure environment (5-15 minutes)
   Reports will be saved in: discovery_output\

================================================================================

❓ TROUBLESHOOTING:

Q: I get "execution policy" or "not digitally signed" error
A: Use run.cmd instead of run_discovery.ps1
   The .cmd file bypasses PowerShell execution policy restrictions

Q: I get "no module named socket" error
A: The bundled Python is incomplete. Use system Python instead:
   1. Double-click: run_system_python.cmd
   OR
   2. Install Python from python.org, then run:
      python -m pip install -r requirements.txt
      python azure_discovery.py

Q: Python not found
A: The bundled Python is in tool\python\ folder
   The script will find it automatically
   OR use system Python (see socket error solution above)

Q: Azure CLI not found
A: Install from: https://aka.ms/installazurecliwindows
   Then run: az login

================================================================================

📚 MORE HELP:

- Open: QUICK_START_GUIDE.html (in your web browser)
- Sample reports: discovery_output\sample_*.html

================================================================================

🔒 SECURITY NOTE:

This tool is 100% READ-ONLY. It only reads your Azure resources and does NOT
create, modify, or delete anything. Safe to run in production environments.

================================================================================
